<p id="notice"><%= notice %></p>

<h1>Listing Offers</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Picture</th>
      <th>Description</th>
      <th>Valid until</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @offers.each do |offer| %>
      <tr>
        <td><%= offer.title %></td>
        <td><%= image_tag(offer.picture_url, :width => 100, :height => 100) if offer.picture.present? %></td>
        <td><%= offer.description %></td>
        <td><%= offer.valid_until %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
